# 1.1.5 智能交通系统的数据采集、处理和审核流程设计 - 题目解析

本题是基础数据处理的“集大成”题目，涵盖了从采集到多维统计的全流程，特别强调了多字段的联合逻辑审核。

---

### 1. 数据采集 (Task 1)

```python
# 读取交通数据
data = pd.read_csv('vehicle_traffic_data.csv')
# 打印预览
print(data.head())
```

### 2. 数据清洗与预处理 (Task 2)

```python
# 删除缺失值记录
data = data.dropna()

# 强制转换数据类型，保证 Speed, Distance 等为浮点数
data['Age'] = data['Age'].astype(int)
data['Speed'] = data['Speed'].astype(float)
data['TravelDistance'] = data['TravelDistance'].astype(float)
data['TravelTime'] = data['TravelTime'].astype(float)

# 异常值剔除：四项业务指标必须全部在合理区间内
data = data[(data['Age'].between(18, 70)) & 
            (data['Speed'].between(0, 200)) & 
            (data['TravelDistance'].between(1, 1000)) & 
            (data['TravelTime'].between(1, 1440))]

# 导出干净的数据集
data.to_csv('cleaned_vehicle_traffic_data.csv', index=False)
```

### 3. 数据合理性审核 (Task 3)

```python
# 利用 ~ (取反) 筛选出不符合业务逻辑的行进行二次复核
unreasonable_data = data[~((data['Age'].between(18, 70)) & 
                           (data['Speed'].between(0, 200)) & 
                           (data['TravelDistance'].between(1, 1000)) & 
                           (data['TravelTime'].between(1, 1440)))]
```

### 4. 数据统计 (Task 4)

```python
# 交通事件频数统计
traffic_event_counts = data['TrafficEvent'].value_counts()

# 分性别聚合：同时计算多个数值列的平均值
gender_stats = data.groupby('Gender')[['Speed', 'TravelDistance', 'TravelTime']].mean()

# 年龄分布：利用 pd.cut 完成分箱
data['AgeGroup'] = pd.cut(data['Age'], bins=age_bins, labels=age_labels, right=False)
age_group_counts = data['AgeGroup'].value_counts().sort_index()
```

---

### 关键点总结：
1.  **逻辑组合**: `&` 连接多个 `between` 是处理复杂业务规则的标准姿势。
2.  **多列聚合**: 在 `groupby` 后的括号 `[[...]]` 中填入多个列名，可以一次性得到多维度的统计表。
3.  **分箱顺序**: `value_counts().sort_index()` 确保年龄段按顺序输出，方便截图和查看。

**操作提醒：**
*   截图 1（前 5 行） -> `1.1.5-1.jpg`。
*   截图 2（合理性审核） -> `1.1.5-2.jpg`。
*   截图 3（事件统计） -> `1.1.5-3.jpg`。
*   截图 4（性别统计） -> `1.1.5-4.jpg`。
*   截图 5（年龄统计） -> `1.1.5-5.jpg`。
*   上传 `cleaned_vehicle_traffic_data.csv`。
*   导出 `1.1.5.html`。
